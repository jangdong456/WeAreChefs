const join_btn = document.getElementById("join_btn");
const member_join = document.getElementsByClassName("member_join");

join_btn.addEventListener("click", ()=> {
    
    for(let a of member_join) {
        
    }


});